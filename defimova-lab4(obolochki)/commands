/**
 *Copyright (c) 2020, Dasha Efimova <dashulya0301178@gmail.com>
 */
  
Шаг 2 (вывод информации):

1)
1.1. ps -Af
1.2. pgrep ps
1.3. ps axu | grep $(whoami)                                      //выведет то, что совпадет с логином
1.4. ps -f -u $(whoami)                                           //работает с процессами только пользователя

2)
2.1. top                                                          //Процессы отображаются в режиме реального времени
2.3. n (далее вводим 5/80/100)                                    //показывает ту часть процессов, что помещается на экране
2.4. Все прописано в первых строках top. Лично у меня:
a)7 users б)5:26 в)211 total г)1 stopped д)1 running е)0 zombie
2.5. u (далее вводим defimova) Лично у меня: top, systemd, (sd-pam), sshd, bash, less)
2.6. k (вводим PID команды top)
2.7. top -b -n 1>top_info

Шаг 3 (сигналы):

1. find / -name '*'>/dev/null                                     //запускаем длительный поиск
2. ps -Af | grep find                                             //узнаем PID команды
kill -s SIGTSTP PID                                               //приостановка выполнения процесса (Ctrl+Z).
kill -l SIGTSTP                                                   //Соответствует число 20 данному сигналу
3. kill -s SIGKILL PID                                            //немедленное прекращение процесса
Также можно: kill -9 PID
SIGKILL нельзя игнорировать или заблокировать. Процесс будет завершен вместе с его потоками

echo $?                                                           //код возврата 0 - процесс успешно "убит"

Шаг4 (задачи):

1. emacs fon &                                                    //&-для фонового режима
2. jobs                                                           //видим, что запущен emacs fon &
3. man ls
Ctrl+Z                                                            //терминальная остановка
4. jobs                                                           //видим, что процесс man ls Остановлен
5. bg %2
6. fg %1
7. kill %1
kill %2